# Override RemoteIP header to X-Forwarded-For for environments where
# CF-Connecting-IP is not preserved by upstream proxies.
# This file name is prefixed with 'zz-' to ensure it loads after other confs.

RemoteIPHeader X-Forwarded-For
